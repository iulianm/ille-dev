const mongoose = require('mongoose');
const { Schema } = mongoose;

const userMemoListSentenceSchema = new Schema({
    // _sentences: [
    //     {
    //         type: Schema.Types.ObjectId,
    //         ref: 'Sentence'
    //     }
    // ]
})

module.exports = userMemoListSentenceSchema;